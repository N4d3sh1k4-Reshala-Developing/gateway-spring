services:
  api-gateway:
    image: ghcr.io/${IMAGE_NAME}:latest
    container_name: api-gateway-reshala
    ports:
      - "8180:8180"
    environment:
      - SERVER_PORT=8180
      - SPRING_PROFILES_ACTIVE=prod
      - EUREKA_CLIENT_ENABLED=false
      - EUREKA_INSTANCE_HOSTNAME=api-gateway-reshala
      - JWT_SECRET_ACCESS=${JWT_SECRET}
      - SERVICES_SECURITY-SERVICE_URI=http://security-service-reshala:8181
      - SERVICES_SOLUTION-ARCHIVE-SERVICE_URI=http://solution-archive-service-reshala:8185
    networks:
      - reshala-net
    restart: unless-stopped

networks:
  reshala-net:
    external: true